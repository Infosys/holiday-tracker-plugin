apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: holiday-tracker-plugin
  description: The Holiday Tracker Plugin for Backstage provides real-time public holiday information for a specified country or region using the Calendarific API. This plugin includes a secure backend proxy configuration to fetch holiday data without exposing your API key.
  annotations:
    github.com/project-slug: Infosys/holiday-tracker-plugin
    backstage.io/techdocs-ref: dir:.
spec:
  type: plugin
  lifecycle: experimental
  owner: group:admin-group
  ownedBy: [group:admin-group, group:dev-group]
